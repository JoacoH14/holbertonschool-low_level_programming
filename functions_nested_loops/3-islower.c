#include"main.h"
/**
 * _islower - nada
 * @c: es una variable que almacena un caracter en ASCII
 * Return: 0
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
		else
		{
	return (0);
	}
	}
