#include"main.h"
/**
 * print_sign - vacio
 * @n: n es una variable que almacena un caracter
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
	return (1);
	}
	else if (n < 0)
	{
		_putchar('-');
		return (-1);
	}
	else
		(n == 0);
	{
		_putchar('0');
		return (0);
}
}
