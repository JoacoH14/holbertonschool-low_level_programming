#include"main.h"
/**
 * _isdigit - nada
 * @c: es una variable que almacena un dato numerico
 * Return: 0
 */
int _isdigit(int c)
{
	if (c >= 1 && c <= 9)
	{
		return (1);
	}
		else
		{
			return (0);
		}
}
