#include"main.h"
/**
 * _isdigit - nada
 * @c: es una variable que almacena un dato numerico
 * Return: 0
 */
int _isdigit(int c)
{
	if (c <= 9 && c >= 0)
	{
		return (1);
	}
		else
		{
			return (0);
		}
}
