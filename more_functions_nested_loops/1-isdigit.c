#include"main.h"
/**
 * _isdigit - nada
 * @c: es una variable que almacena un dato numerico
 * Return: 0
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (0);
	}
		else
		{
			return (1);
		}
}
